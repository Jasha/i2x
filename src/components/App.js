import React from 'react';
import './app.css';

class AppComponent extends React.Component {
  render() {
    return (
      <div>Please code something...</div>
    );
  }
}

AppComponent.defaultProps = {
};

export default AppComponent;
